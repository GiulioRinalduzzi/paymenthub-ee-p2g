# Coordinates are declared once here instead of in each module, so the two
# deployables can never drift apart on group or version.
group=org.mifos
version=2.0.0.mifos-SNAPSHOT

org.gradle.jvmargs=-Xmx1024m -Dfile.encoding=UTF-8
